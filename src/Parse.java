import java.util.ArrayList;

public class Parse{
	public Node<String> ParseList(ArrayList<String> lista) {
		for(int i = 0; i < lista.size(); i++) {
            
        }
		Node<String> root = new Node<String>("A");
		return root;
   }
}
